#include <cstddef>
#include <iostream>
#include <map>

#include "17/17.h"
#include "shared/shared.h"

using namespace std;

Input ParseInput(const string &filename) { return ReadLines(filename); }

size_t PartOne(Input input)
{
    size_t ret = 0;
    return ret;
}

size_t PartTwo(Input input)
{
    size_t ret = 0;
    return ret;
}